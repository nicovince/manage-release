name: Build on push
on: [push]

jobs:
  rolling_release:
    runs-on: ubuntu-latest
    name: Rolling Release on Branch
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Release
        uses: nicovince/manage-release@c040c754145c3ed32a96c00daa8a2acfcca42940
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          files: manage-release.sh
